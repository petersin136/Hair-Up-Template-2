/** Shared section heights — must stay outside "use client" modules
 *  so Server Components can use them in layout math. */
export const HERO_HEIGHT = 3321;
export const SERVICES_SECTION_HEIGHT = 1108;
export const BOOKING_SECTION_HEIGHT = 1401;
export const FEATURE_VIDEO_HEIGHT = 720;
/** REVIEW + Instagram strip (measured from 05 mockups → 1440). */
export const REVIEW_SECTION_HEIGHT = 1368;
/** HOURS / LOCATION band (06 mockup, above divider). */
export const LOCATION_SECTION_HEIGHT = 713;
/** Footer band below divider. */
export const FOOTER_SECTION_HEIGHT = 366;


